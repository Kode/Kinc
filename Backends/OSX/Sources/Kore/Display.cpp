#include "pch.h"

#include <Kore/Log.h>

namespace Kore { namespace Display {
    int count() {
        log(Warning, "TODO (DK) implement me");
        return 1;
    }
    
    int width( int index ) {
        log(Warning, "TODO (DK) implement me");
        return -1;
    }
    
    int height( int index ) {
        log(Warning, "TODO (DK) implement me");
        return -1;
    }
}}
